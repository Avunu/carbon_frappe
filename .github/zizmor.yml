# zizmor — static analysis of the workflows. Third-party actions must be
# hash-pinned (dependabot keeps the hashes moving); GitHub's own may ride a
# major tag.
rules:
  unpinned-uses:
    config:
      policies:
        "actions/*": ref-pin
        "github/*": ref-pin
        "*": hash-pin
